using System;

namespace ProjetoTesteBackEnd
{
    public class Operacoes
    {
        public static double Somar(double pNum, double sNum)
        {
            return pNum + sNum;
            //  Pode ser escrito return (pNum + sNum);
        }
    }
}
